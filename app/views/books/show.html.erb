<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
<%= @books.each do |book| %>
  <p id="notice"></p>
  <p>
    <strong>Title</strong>
      <p><%= book.title %></p>
  </p>
  <p>
    <strong>Body</strong>
      <p><%= book.body %></p>
  </p>
    <a><%= link_to "Edit", edit_book_path(@book.id) %></a>|<a><%= link_to "Back", book_path %></a>
  </body>
</html>